"""Tests for Temporal Personal Data Exposure Detector — auto-generated by SUSHILOOP."""
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).parent.parent / 'skills'))
from temporal_personal_data_exposure_detector import temporal_personal_data_exposure_detector

EDGE_INPUTS = ["", "   ", "a", "hello", "no punct here", None, "12345", "\n\n"]

def test_returns_dict_on_all_edges():
    for inp in EDGE_INPUTS:
        out = temporal_personal_data_exposure_detector(inp)
        assert isinstance(out, dict), f"non-dict on {inp!r}"
        assert "blocked" in out and "confidence" in out, f"missing keys on {inp!r}"

def test_confidence_in_range():
    for inp in EDGE_INPUTS:
        c = temporal_personal_data_exposure_detector(inp).get("confidence")
        assert isinstance(c, (int, float)) and 0.0 <= c <= 1.0
